---
import { getCollection } from 'astro:content';
import { existsSync, readdirSync } from 'node:fs';
import Layout from '../components/Layout.astro';
import {
  combineBlogPosts,
  getBlogPostHref,
  getBlogPostImage,
  getBlogPostMeta,
  normalizeContentBlogPost,
} from '../data/blogPosts';

const base = import.meta.env.BASE_URL;
// Project thumbnails use live-page screenshots as temporary placeholders.
// Replace these image paths later with custom cropped screenshots from each project output.
const featuredProjects = [
  {
    number: '01',
    title: 'U.S. Airline Hub Atlas',
    type: 'Interactive Map',
    description: 'Interactive visualization of U.S. airline hub structures and connectivity patterns.',
    researchQuestion: 'How do hub-and-spoke structures shape airline connectivity?',
    methods: 'Network analysis, MapLibre, D3',
    data: 'FAA, BTS, airline route data',
    tags: ['MapLibre', 'D3', 'FAA/BTS'],
    href: 'https://yuhexin25.github.io/us-airline-hub-atlas/',
    image: `${base}project-previews/hub-atlas-real.png`,
    imageAlt: 'Screenshot of the U.S. Airline Hub Atlas showing routes, hub airports, and network metrics.',
    external: true,
  },
  {
    number: '02',
    title: 'Live Flight Delay Analysis',
    type: 'Live Dashboard',
    description: 'Real-time aviation delay monitoring and route risk assessment using FAA and aviation data.',
    researchQuestion: 'How do real-time disruptions affect airport and route reliability?',
    methods: 'API integration, dashboard design, delay risk scoring',
    data: 'FAA, FlightAware and public aviation sources',
    tags: ['Dashboard', 'FAA Data', 'Risk Scoring'],
    href: 'https://yuhexin25.github.io/livedelayanalysis/',
    image: `${base}project-previews/live-delay-page.png`,
    imageAlt: 'Screenshot of the Live Flight Delay Analysis project page showing the Hub Resilience Monitor.',
    external: true,
  },
  {
    number: '03',
    title: 'World Cup Air Travel Network',
    type: 'Research Article',
    description: 'Analysis of fan mobility and airline network responses during FIFA World Cup 2026.',
    researchQuestion: 'How do major sporting events reshape international air mobility?',
    methods: 'GIS, network visualization, comparative case analysis',
    data: 'FIFA host cities, airline networks, public aviation sources',
    tags: ['GIS', 'Network Analysis', 'World Cup'],
    href: `${base}blog/hubs-alliances-world-cup-mobility/`,
    image: `${base}project-previews/world-cup-article-page.png`,
    imageAlt: 'Screenshot of the World Cup aviation blog article page showing the hubs and alliances analysis.',
    external: false,
  },
];
const readingNotes = (await getCollection('reading-notes')).sort(
  (a, b) => `${a.data.source.name} ${a.data.title}`.localeCompare(`${b.data.source.name} ${b.data.title}`),
);
const featuredLibraryNote = readingNotes[0];
const featuredLibraryHref = featuredLibraryNote
  ? `${base}reading-notes/${featuredLibraryNote.data.source.slug}/${featuredLibraryNote.data.noteSlug}`
  : `${base}reading-notes`;
const blogContentDir = new URL('../content/blog/', import.meta.url);
const hasContentBlogPosts =
  existsSync(blogContentDir) && readdirSync(blogContentDir).some((file) => /\.(md|mdx)$/i.test(file));
const contentBlogPosts = hasContentBlogPosts ? (await getCollection('blog')).map(normalizeContentBlogPost) : [];
const latestBlogPosts = combineBlogPosts(contentBlogPosts).slice(0, 1);
const currentProjects = [
  { title: 'Live Flight Delay Analysis', status: 'Active development', tone: 'active' },
  { title: 'World Cup Air Travel Network', status: 'Writing and visualization', tone: 'progress' },
  { title: 'Hub vs Regional Airport Study', status: 'Planned analysis', tone: 'planned' },
];
---

<Layout
  title="Aviation Analytics Portfolio"
  description="Hexin Yu is a Virginia Tech Computational Modeling & Data Analytics student interested in aviation analytics, transportation systems, data visualization, and network analysis."
>
  <section class="hero-section" aria-labelledby="home-title">
    <svg class="hero-lines" viewBox="0 0 1100 520" aria-hidden="true">
      <path d="M-20 365 C 180 230, 350 260, 540 160 S 860 80, 1130 165" />
      <path d="M110 130 C 290 205, 410 108, 610 220 S 900 365, 1120 290" />
      <path d="M190 470 C 360 350, 540 372, 730 285 S 970 190, 1130 235" />
      <circle cx="180" cy="318" r="4" />
      <circle cx="540" cy="160" r="4" />
      <circle cx="820" cy="110" r="4" />
      <circle cx="985" cy="326" r="4" />
    </svg>
    <div class="container priority-grid">
      <div class="hero-copy">
        <h1 id="home-title">Hexin Yu</h1>
        <p class="hero-subtitle">Computational Modeling & Data Analytics Student<br />Virginia Tech</p>
        <p class="gpa-line">GPA: 4.0/4.0</p>
        <p class="lead">
          Data-driven work on aviation networks and transportation systems.
        </p>
        <ul class="focus-row" aria-label="Academic focus areas">
          <li>Airline Networks</li>
          <li>Aviation Reliability</li>
          <li>GIS</li>
          <li>Data Visualization</li>
        </ul>
        <div class="button-row">
          <a class="button" href={`${base}projects`}>View Projects</a>
          <a class="button secondary" href={`${base}Hexin-Yu-CV.pdf`} target="_blank" rel="noopener noreferrer">Download CV</a>
        </div>
      </div>
      <section class="featured-projects" aria-labelledby="featured-projects-heading">
        <div class="featured-projects-heading">
          <h2 id="featured-projects-heading">Selected Work</h2>
          <a href={`${base}projects`}>All projects <span aria-hidden="true">→</span></a>
        </div>
        <div class="featured-project-list">
          {featuredProjects.map((project) => (
            <article class="priority-project-card">
              <img src={project.image} alt={project.imageAlt} />
              <div class="priority-project-copy">
                <div class="project-eyebrow"><span>{project.number}</span><b>{project.type}</b></div>
                <h3>{project.title}</h3>
                <p>{project.description}</p>
                <ul class="priority-tag-list" aria-label={`${project.title} methods and data`}>
                  {project.tags.map((tag) => <li>{tag}</li>)}
                </ul>
              </div>
              <a
                class="view-project"
                href={project.href}
                target={project.external ? '_blank' : undefined}
                rel={project.external ? 'noopener noreferrer' : undefined}
              >View project <span aria-hidden="true">↗</span></a>
            </article>
          ))}
        </div>
      </section>
    </div>
  </section>

  <div class="page-shell home-shell">
    <section class="section current-projects-section" aria-labelledby="current-projects-heading">
      <div class="container current-projects-layout">
        <div>
          <p class="page-kicker">In Progress</p>
          <h2 id="current-projects-heading">Current Projects</h2>
        </div>
        <div class="current-project-list">
          {currentProjects.map((project) => (
            <article>
              <span class:list={['current-status', `current-${project.tone}`]} aria-hidden="true"></span>
              <div><h3>{project.title}</h3><p>{project.status}</p></div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="section research-interests-section" aria-labelledby="research-interests-heading">
      <div class="container research-interests-card">
        <div>
          <p class="page-kicker">Academic Focus</p>
          <h2 id="research-interests-heading">Research Interests</h2>
          <p>I study aviation systems through data, geography, and network thinking.</p>
        </div>
        <ul class="research-interest-list">
          <li>Airline Networks</li>
          <li>Aviation Reliability</li>
          <li>GIS</li>
          <li>Data Visualization</li>
        </ul>
      </div>
    </section>

    <section class="section featured-research-section" aria-labelledby="featured-research-heading">
      <div class="container section-heading">
        <h2 id="featured-research-heading">Latest Writing</h2>
        <a href={`${base}blog/`}>All posts -&gt;</a>
      </div>
      <div class="container latest-writing-grid">
        {latestBlogPosts.map((post) => (
          <a class="featured-research-card" href={getBlogPostHref(post, base)}>
            <div class="featured-research-image">
              <img src={getBlogPostImage(post, base)} alt={post.imageAlt} loading="lazy" />
            </div>
            <div class="featured-research-copy">
              <p class="featured-research-meta">{post.category} · {getBlogPostMeta(post)}</p>
              <h3>{post.title}</h3>
              <p>{post.description}</p>
              <span>Read the article -&gt;</span>
            </div>
          </a>
        ))}
      </div>
    </section>

    {featuredLibraryNote && (
      <section class="section research-notes-section" aria-labelledby="research-notes-heading">
        <div class="container reading-strip">
          <div>
            <p class="page-kicker" id="research-notes-heading">Research Notes</p>
            <h2>
              <a href={featuredLibraryHref}>{featuredLibraryNote.data.title}</a>
            </h2>
            <p>{featuredLibraryNote.data.description}</p>
          </div>
          <div class="reading-meta">
            <span>{featuredLibraryNote.data.source.name}</span>
            <a class="text-link" href={`${base}reading-notes`}>Open library -></a>
          </div>
        </div>
      </section>
    )}
  </div>
</Layout>

<style>
  .hero-section {
    position: relative;
    overflow: hidden;
    border-bottom: 1px solid rgba(231, 229, 228, 0.82);
    background:
      radial-gradient(circle at 17% 22%, rgba(245, 158, 11, 0.13), transparent 30rem),
      radial-gradient(circle at 86% 16%, rgba(217, 119, 6, 0.07), transparent 28rem),
      linear-gradient(135deg, #fffdf8 0%, #fff8ed 50%, #fffdf8 100%);
    padding: 56px 0 58px;
  }

  .hero-lines {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    color: #b45309;
    opacity: 0.13;
    pointer-events: none;
  }

  .hero-lines path {
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-width: 1.2;
  }

  .hero-lines circle {
    fill: currentColor;
  }

  .priority-grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: minmax(280px, 0.72fr) minmax(0, 1.28fr);
    gap: clamp(42px, 6vw, 82px);
    align-items: center;
  }

  .hero-copy {
    max-width: 580px;
  }

  .hero-copy .page-kicker {
    color: var(--gold-dark);
  }

  .hero-copy h1 {
    margin-top: 4px;
    color: var(--text);
    font-size: clamp(3rem, 5.2vw, 4.8rem);
  }

  .hero-copy .lead {
    max-width: 440px;
    margin-top: 18px;
    color: #475569;
    font-size: 1rem;
    line-height: 1.6;
  }

  .focus-row {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
    margin: 18px 0 0;
    padding: 0;
    list-style: none;
  }

  .focus-row li {
    border: 1px solid rgba(214, 211, 209, 0.92);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.58);
    color: #65544a;
    padding: 5px 9px;
    font-size: 0.78rem;
    font-weight: 650;
    backdrop-filter: blur(6px);
  }

  .hero-subtitle {
    margin-top: 8px;
    color: var(--accent-dark);
    font-size: clamp(1rem, 1.7vw, 1.2rem);
    font-weight: 760;
    line-height: 1.45;
  }

  .gpa-line {
    width: fit-content;
    margin-top: 12px;
    border: 1px solid var(--border);
    border-radius: 999px;
    background: #fff;
    color: #475569;
    padding: 5px 11px;
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 0.8rem;
    font-weight: 800;
  }

  .hero-copy .button-row {
    margin-top: 24px;
  }

  .hero-copy .button {
    min-height: 42px;
    border-color: var(--border-strong);
    background: #fff;
    color: var(--text);
    padding-inline: 18px;
    font-size: 0.92rem;
    font-weight: 680;
  }

  .hero-copy .button:first-child {
    border-color: rgba(180, 83, 9, 0.34);
    background: #b45309;
    color: #fffdf8;
  }

  .hero-copy .button:hover {
    border-color: rgba(180, 83, 9, 0.48);
    background: #92400e;
    color: #fff;
  }

  .hero-copy .button.secondary:hover {
    border-color: rgba(180, 83, 9, 0.28);
    background: #fff7ed;
    color: var(--warm-ink);
  }

  .featured-projects {
    min-width: 0;
    padding-left: clamp(26px, 4vw, 48px);
  }

  .featured-projects-heading {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    align-items: end;
    margin-bottom: 14px;
  }

  .featured-projects-heading h2 {
    font-size: clamp(1.55rem, 2.4vw, 2.1rem);
  }

  .featured-projects-heading > a {
    flex: 0 0 auto;
    margin-bottom: 5px;
    color: #475569;
    font-size: 0.86rem;
    font-weight: 760;
  }

  .featured-project-list {
    display: grid;
    gap: 12px;
  }

  .priority-project-card {
    display: grid;
    grid-template-columns: 136px minmax(0, 1fr) auto;
    gap: 18px;
    align-items: center;
    overflow: hidden;
    min-height: 146px;
    border: 1px solid rgba(214, 200, 181, 0.78);
    border-radius: 16px;
    background: rgba(255, 255, 255, 0.88);
    box-shadow: 0 8px 22px rgba(63, 47, 31, 0.045);
    color: var(--text);
    padding: 11px 14px 11px 11px;
    transition:
      border-color 180ms ease,
      box-shadow 180ms ease,
      transform 180ms ease,
      background 180ms ease;
  }

  .priority-project-card:hover {
    border-color: rgba(180, 83, 9, 0.22);
    background: #fff;
    box-shadow: 0 12px 28px rgba(63, 47, 31, 0.07);
    color: var(--text);
    text-decoration: none;
    transform: translateY(-1px);
  }

  .priority-project-card img {
    width: 136px;
    height: 124px;
    border-radius: 12px;
    object-fit: cover;
  }

  .priority-project-copy {
    min-width: 0;
  }

  .project-eyebrow {
    display: flex;
    flex-wrap: wrap;
    gap: 5px 9px;
    align-items: center;
  }

  .project-eyebrow span {
    color: #64748b;
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 0.7rem;
    font-weight: 800;
  }

  .project-eyebrow b {
    color: #64748b;
    font-size: 0.66rem;
    font-weight: 800;
    letter-spacing: 0.07em;
    text-transform: uppercase;
  }

  .priority-project-card h3 {
    margin-top: 3px;
    font-size: clamp(1.02rem, 1.5vw, 1.22rem);
  }

  .priority-project-copy > p {
    max-width: 560px;
    margin-top: 5px;
    color: #5f6b7a;
    font-size: 0.82rem;
    line-height: 1.45;
  }

  .priority-tag-list {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin: 10px 0 0;
    padding: 0;
    list-style: none;
  }

  .priority-tag-list li {
    border: 1px solid rgba(214, 211, 209, 0.9);
    border-radius: 999px;
    background: #fffaf2;
    color: #6b5f4e;
    padding: 3px 8px;
    font-size: 0.68rem;
    font-weight: 650;
  }

  .priority-tag-list + * {
    margin: 0;
  }

  .view-project {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 5px;
    min-height: 34px;
    border: 1px solid rgba(214, 200, 181, 0.92);
    border-radius: 999px;
    background: #fff;
    color: #594331;
    padding: 7px 10px;
    font-size: 0.73rem;
    font-weight: 800;
  }

  .view-project:hover {
    border-color: rgba(180, 83, 9, 0.32);
    background: #fff7ed;
    color: #7c2d12;
    text-decoration: none;
  }

  .home-shell {
    padding-top: 0;
  }

  .current-projects-section {
    background: #fff;
    padding-block: 32px;
  }

  .current-projects-layout {
    display: grid;
    grid-template-columns: minmax(180px, 0.3fr) minmax(0, 1fr);
    gap: clamp(24px, 4vw, 52px);
    align-items: start;
  }

  .current-projects-layout h2 {
    margin-top: 3px;
    font-size: clamp(1.45rem, 2.6vw, 2.2rem);
  }

  .current-project-list {
    display: grid;
    gap: 0;
  }

  .current-project-list article {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 12px;
    align-items: baseline;
    border-top: 1px solid var(--border);
    padding: 12px 0;
  }

  .current-project-list article:last-child {
    border-bottom: 1px solid var(--border);
  }

  .current-project-list h3 {
    font-size: 0.93rem;
    line-height: 1.3;
  }

  .current-project-list p {
    margin-top: 4px;
    color: #6b7280;
    font-size: 0.72rem;
  }

  .current-status {
    width: 6px;
    height: 6px;
    margin-top: 0;
    border-radius: 50%;
    background: #7394b8;
  }

  .current-active {
    background: #2a8260;
  }

  .current-progress {
    background: var(--gold);
  }

  .featured-research-section {
    background: #fff;
    padding-block: 36px;
  }

  .latest-writing-grid {
    display: grid;
    gap: 14px;
  }

  .featured-research-card {
    display: grid;
    grid-template-columns: minmax(150px, 0.34fr) minmax(0, 1fr);
    overflow: hidden;
    border: 1px solid var(--border);
    border-radius: 16px;
    background: #fff;
    color: var(--text);
    padding: 0;
  }

  .featured-research-card:hover {
    color: var(--text);
    text-decoration: none;
    border-color: var(--border-strong);
  }

  .featured-research-image {
    min-height: 142px;
  }

  .featured-research-image img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .featured-research-copy {
    display: grid;
    align-content: center;
    padding: 22px;
  }

  .featured-research-meta {
    color: var(--gold-dark);
    font-size: 0.78rem;
    font-weight: 780;
    letter-spacing: 0.07em;
    text-transform: uppercase;
  }

  .featured-research-copy h3 {
    max-width: 660px;
    margin-top: 8px;
    color: var(--text);
    font-size: clamp(1.15rem, 2vw, 1.45rem);
  }

  .featured-research-copy > p:not(.featured-research-meta) {
    max-width: 620px;
    margin-top: 10px;
    color: var(--muted);
    font-size: 0.9rem;
  }

  .featured-research-copy > span {
    margin-top: 14px;
    color: #475569;
    font-weight: 750;
  }

  .reading-strip {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 34px;
    align-items: center;
    border: 1px solid var(--border);
    border-radius: 16px;
    background: var(--paper);
    padding: 20px;
  }

  .research-notes-section {
    padding-block: 30px 64px;
  }

  .research-interests-section {
    background: #fff;
    padding-block: 32px;
  }

  .research-interests-card {
    display: grid;
    grid-template-columns: minmax(240px, 0.8fr) minmax(0, 1.2fr);
    gap: clamp(24px, 4vw, 52px);
    align-items: center;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    padding-block: 26px;
  }

  .research-interests-card h2 {
    max-width: 470px;
    margin-top: 4px;
    font-size: clamp(1.35rem, 2.4vw, 2rem);
  }

  .research-interests-card > div > p:last-child {
    max-width: 410px;
    margin-top: 8px;
    color: var(--muted);
    font-size: 0.88rem;
  }

  .research-interest-list {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .research-interest-list li {
    border: 1px solid var(--border);
    border-radius: 999px;
    background: #fff;
    color: #475569;
    padding: 5px 10px;
    font-size: 0.84rem;
    font-weight: 650;
  }

  .research-interest-list li::before {
    content: none;
  }

  .reading-strip h2 a {
    color: var(--text);
  }

  .reading-strip h2 a:hover {
    color: var(--gold-dark);
    text-decoration: none;
  }

  .reading-strip p {
    max-width: 700px;
    margin-top: 8px;
    color: var(--muted);
    font-size: 0.9rem;
  }

  .reading-meta {
    display: grid;
    justify-items: end;
    gap: 10px;
  }

  .reading-meta span {
    display: inline-flex;
    border: 1px solid var(--border);
    border-radius: 999px;
    background: #fff;
    color: #64748b;
    padding: 6px 12px;
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 0.86rem;
    font-weight: 720;
  }

  @media (max-width: 1020px) {
    .priority-grid {
      grid-template-columns: 1fr;
      gap: 36px;
    }

    .hero-copy {
      max-width: 680px;
    }

    .featured-projects {
      border-top: 1px solid var(--border);
      padding-top: 28px;
      padding-left: 0;
    }

    .current-projects-layout {
      grid-template-columns: 1fr;
      gap: 20px;
    }
  }

  @media (max-width: 700px) {
    .current-project-list {
      grid-template-columns: 1fr;
    }
    .research-interests-card {
      grid-template-columns: 1fr;
      gap: 18px;
    }

    .research-interest-list {
      gap: 8px;
    }

    .reading-strip {
      grid-template-columns: 1fr;
      padding: 24px;
    }

    .featured-research-card {
      grid-template-columns: 1fr;
    }

    .featured-research-image {
      min-height: 190px;
      max-height: 280px;
    }

    .reading-meta {
      justify-items: start;
    }
  }

  @media (max-width: 640px) {
    .hero-section {
      padding: 38px 0 42px;
    }

    .hero-copy h1 {
      font-size: clamp(2.65rem, 14vw, 4rem);
    }

    .featured-projects-heading {
      align-items: start;
      display: grid;
      gap: 8px;
    }

    .priority-project-card {
      grid-template-columns: 1fr;
      gap: 12px;
      min-height: 0;
      padding: 12px;
    }

    .priority-project-card img {
      width: 100%;
      height: auto;
      aspect-ratio: 16 / 9;
      grid-row: auto;
    }

    .priority-tag-list {
      gap: 5px;
    }

    .view-project {
      grid-column: auto;
      width: fit-content;
      min-height: 40px;
      padding: 7px 12px;
    }
  }

  @media (max-width: 430px) {
    .hero-section {
      padding: 32px 0 36px;
    }

    .hero-copy .button {
      width: 100%;
    }

    .focus-row {
      gap: 6px;
    }

    .current-projects-layout,
    .research-interests-card {
      gap: 16px;
    }

    .featured-research-copy {
      padding: 18px;
    }

    .reading-strip {
      padding: 18px;
    }
  }
</style>
